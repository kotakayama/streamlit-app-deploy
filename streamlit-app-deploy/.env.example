# Example environment variables for Valuation App
# Port the Streamlit app listens on
STREAMLIT_PORT=8501

# If you have installed Tesseract in a non-standard location, set this
# TESSERACT_CMD=C:\\Program Files\\Tesseract-OCR\\tesseract.exe
# Language model for pytesseract (if needed)
# TESSERACT_LANG=jpn+eng

# OpenAI API key (DO NOT commit real keys to the repository)
# Put your actual key in a local `.env` file or set it in your deployment environment.
# Example (replace with your real key locally):
# OPENAI_API_KEY=sk-xxxxxxx
OPENAI_API_KEY=your_openai_api_key_here